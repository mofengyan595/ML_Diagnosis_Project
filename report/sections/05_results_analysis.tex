\section{结果与分析}

\subsection{模型性能比较}

表 \ref{tab:model_comparison} 是最终报告的核心结果表。
目前项目文件中仅存在 Logistic Regression 基准模型结果，
因此只填写该行的真实指标；
其他模型必须在训练结果产生后再补充，不能编造数值。

\begin{table*}[t]
  \caption{模型性能比较表}
  \label{tab:model_comparison}
  \centering
  \scriptsize
  \begin{tabular}{lcccccccccc}
    \toprule
    模型
    & Val Acc. & Val Prec. & Val Recall & Val F1 & Val AUC
    & Test Acc. & Test Prec. & Test Recall & Test F1 & Test AUC \\
    \midrule
    Logistic Regression
    & 0.7642 & 0.6346 & 0.7674 & 0.6947 & 0.8709
    & 0.7273 & 0.5938 & 0.7037 & 0.6441 & 0.8154 \\
    KNN & \multicolumn{10}{c}{待训练并补充} \\
    Decision Tree & \multicolumn{10}{c}{待训练并补充} \\
    Random Forest & \multicolumn{10}{c}{待训练并补充} \\
    SVM & \multicolumn{10}{c}{待训练并补充} \\
    XGBoost/MLP & \multicolumn{10}{c}{可选，按实际完成情况补充或删除} \\
    \bottomrule
  \end{tabular}
\end{table*}

基准模型在测试集上的 ROC AUC 为 0.8154，
说明其概率排序能力达到可用水平；
但测试集 Recall 为 0.7037，仍存在漏判阳性样本的风险。
后续模型选择应重点观察复杂模型是否能在不显著牺牲 Precision 的前提下
提高 Recall、F1-score 和 ROC AUC。

\placeholder{补充多模型比较结论。
需要回答：最佳模型是谁、是否明显优于 Logistic Regression、
是否存在 Precision/Recall 取舍、最终 Demo 为什么选择该模型。}

\subsection{误判与 ROC 分析}

当前基准模型在验证集上的混淆矩阵为：
TN=61、FP=19、FN=10、TP=33；
在测试集上为：
TN=74、FP=26、FN=16、TP=38。
从筛查任务角度看，16 个测试集假阴性意味着真实阳性样本被判断为阴性，
这是后续模型优化时应重点减少的错误类型。

\placeholder{补充最佳模型的混淆矩阵热力图和 ROC 曲线。
如果已生成多模型 ROC 曲线，可在此比较各模型 AUC 和曲线形态。}

\subsection{特征解释}

当前可用的解释结果来自 Logistic Regression 基准模型。
图 \ref{fig:baseline_feature_importance} 展示了标准化系数绝对值排序。
Glucose、BMI、DiabetesPedigreeFunction、Pregnancies 和 Age 位于前五位；
其中 Glucose、BMI 和 Age 与相关研究中强调的重要因素基本一致
\cite{LiJiaSiJiYuJiQiXueXiDeTangNiaoBingYuCeJiSHAPTeZhengFenXi2023,
ChengLuPingJiYuJiQiXueXiDe2XingTangNiaoBingFaBingYinSuFenXiYuYuCe2025}。
需要注意的是，线性模型系数只反映当前数据划分和模型假设下的统计关联，
不能解释为医学因果关系。

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{baseline_feature_importance.png}
  \caption{Logistic Regression 基准模型特征影响排序}
  \label{fig:baseline_feature_importance}
\end{figure}

\placeholder{若最终模型为 Random Forest、XGBoost 或其他非线性模型，
在此替换或补充特征重要性、置换重要性或 SHAP 图。}

\subsection{系统演示}

为展示模型在单样本预测场景中的应用效果，
本文进一步构建了基于 Gradio 的糖尿病风险预测问诊系统。
系统输入与 Pima 数据集的 8 个特征保持一致，
包括 Pregnancies、Glucose、BloodPressure、SkinThickness、
Insulin、BMI、DiabetesPedigreeFunction 和 Age。
在交互界面中，这些变量被转换为易于理解的中文表单项，
用户可以根据个体情况填写或调整相应数值。
系统同时保留典型样例入口，
用于在展示时快速说明低风险和高风险输入在模型输出上的差异。

系统预测流程严格复用实验阶段的预处理协议。
对于网页端提交的原始指标，系统首先按照训练阶段的规则处理不合理 0 值，
再使用训练集拟合得到的中位数填充器、IQR 裁剪边界和标准化器进行变换。
完成预处理后，样本被送入实验阶段选定的分类模型，
模型输出糖尿病阳性的预测概率。
由于预处理对象和模型对象均由训练阶段保存得到，
系统端推理与离线实验保持了相同的特征顺序和数据变换方式，
从而提高了系统展示结果与实验结果之间的一致性。

系统输出由三部分组成。
第一部分是阳性预测概率，用于表示模型认为该输入样本属于糖尿病阳性类别的可能性；
第二部分是风险等级，
本文按概率阈值将低于 0.35 的样本标记为低风险，
将位于 $[0.35,0.65)$ 的样本标记为中风险，
将不低于 0.65 的样本标记为高风险；
第三部分是简要解释文本，
用于提示血糖、BMI、年龄、血压或糖尿病家族遗传指数等较突出的输入因素。
因此，系统展示并不只给出二分类标签，
而是同时呈现概率、等级和输入因素说明，
使预测结果更适合课程展示和非技术用户理解。

该系统仅用于机器学习课程项目展示，
其输出不能替代医生诊断或真实临床决策。
